// Every skin's CSS + the shared compositor structure, as one side-effect import.
// Loaded by BOTH the website (App) and the desktop widget (WidgetApp) so the
// player only renders correctly in one place if it renders correctly in both —
// and a new skin's palette is added here once, not in two component files.
import "./app.css";
import "./player.css";
import "./winamp.css";
import "./frog.css";
import "./burger.css";
import "./bondi.css";
import "./toilet.css";
import "./biomech.css";
import "./wmp.css";
import "./halo.css";
