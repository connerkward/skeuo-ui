{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Desktop-only capabilities: the window-state plugin persists the widget's position/size. iOS has no window-state plugin, so this is scoped to desktop platforms.",
  "platforms": ["macOS", "windows", "linux"],
  "windows": ["main"],
  "permissions": [
    "window-state:default"
  ]
}
